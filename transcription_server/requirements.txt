fastapi>=0.115.0,<0.116
# NOTE: deliberately NOT uvicorn[standard] — the [standard] extras (uvloop,
# httptools, watchfiles) have no prebuilt wheels for Termux/Android and
# watchfiles' build backend requires Rust. This server targets Termux, and
# the extras are pure performance tuning we don't need on a LAN service.
uvicorn
# pydantic v1 line: pydantic v2 needs pydantic-core, a Rust extension with
# no prebuilt Android wheels (and compiling it needs maturin+cargo). v1 is
# pure Python and fully supported by FastAPI — this keeps the Termux
# install 100% compiler-free.
# fastapi is pinned to <0.116 because newer FastAPI releases dropped
# pydantic v1 support in code while still advertising v1 compatibility in
# their package metadata, which silently breaks at import time.
pydantic>=1.10,<2
httpx
aiosqlite
aiofiles
python-dotenv
